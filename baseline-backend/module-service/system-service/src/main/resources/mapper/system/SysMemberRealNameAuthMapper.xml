<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.baseline.system.mapper.SysMemberRealNameAuthMapper">

    <select id="selectByMemberId" resultType="com.baseline.system.entity.SysMemberRealNameAuth">
        select * from sys_member_real_name_auth
        where member_id = #{memberId} and deleted = 0
        limit 1
    </select>

    <select id="selectByCertNoHash" resultType="com.baseline.system.entity.SysMemberRealNameAuth">
        select * from sys_member_real_name_auth
        where cert_no_hash = #{certNoHash} and deleted = 0
        limit 1
    </select>

    <select id="checkMemberAuth" resultType="boolean">
        select count(*) > 0 from sys_member_real_name_auth
        where member_id = #{memberId} and auth_status = 1 and deleted = 0
    </select>

</mapper>
